# playground-nestjs

NestJS 학습·실험용 개인 플레이그라운드. 프로덕션 코드 아님.

---

## Stack

- 아직 미정 — NestJS 앱 스캐폴딩 전
- 언어/프레임워크: TypeScript + NestJS (예정)
- 패키지 매니저: (미정)

## Dev

```bash
# 스캐폴딩 후 채운다
```

환경변수: `.env` (`.env.example` 참고) — 아직 없음

## Structure

```
docs/          # ai-workflow 문서 세트 (TASKS / DECISIONS / INVARIANTS / adr)
```

## 프로젝트 컨벤션

글로벌 rules와 **다른 것만** 명시. 현재 없음.

## 에이전트 힌트

- 개인 실험 레포다. 실험적 구현을 버리는 비용이 낮으므로 위임/시행착오에 관대하게.
- NestJS 앱 스캐폴딩은 아직 안 했다. 코드 작업 요청 시 먼저 `nest new` 여부를 확인한다.

---

> 코딩 스타일, 에이전트 목록, 보안 체크리스트, Git 컨벤션은
> 글로벌 `~/.claude/` 설정에 있으므로 여기서 반복하지 않는다.
